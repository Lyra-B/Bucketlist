$(document).ready(function(){
  map = new BucketListMap(38.9067338, 1.4205982,4);
  map.drawMap('mapCanvas');
  var travellerId = $('#map-canvas').data("traveller_id");
  map.loadMarkers(travellerId);
});
